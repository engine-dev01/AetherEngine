package io.flutter.embedding.engine;

import io.flutter.plugin.common.BinaryMessenger;

/** shape-only stub — full_compile gate; mirror dartExecutor/binaryMessenger ที่
 *  AetherHostActivity.configureFlutterEngine ใช้จริง (flutter 3.22 embedding v2) */
public final class FlutterEngine {
  public DartExecutor getDartExecutor() { return null; }
}
